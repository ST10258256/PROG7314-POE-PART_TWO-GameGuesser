<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#2E2157"
    tools:context=".ui.settings.DeleteAccountFragment">

    <!-- Page Title -->
    <TextView
        android:id="@+id/settingsTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Account deletion"
        android:textColor="@android:color/white"
        android:textSize="26sp"
        android:textStyle="bold"
        android:paddingBottom="20dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="32dp" />

    <LinearLayout
        android:id="@+id/settingsContainer"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:background="@drawable/rounded_top_bg"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:paddingBottom="24dp"
        app:layout_constraintTop_toBottomOf="@+id/settingsTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">

        <!-- Confirmation question -->
        <TextView
            android:id="@+id/tvDeleteQuestion"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Are you sure you want to delete your account?"
            android:textColor="@android:color/white"
            android:textSize="24sp"
            android:paddingTop="20dp"
            android:textStyle="bold"
            android:gravity="center"
            android:layout_marginBottom="16dp"/>

        <!-- Explanation text -->
        <TextView
            android:id="@+id/tvDeleteExplanation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Deleting your profile will permanently remove all your personal data and game progress. This action cannot be undone."
            android:textColor="@color/black"
            android:layout_margin="10dp"
            android:textSize="16sp"
            android:background="@drawable/textbox_bg_format"
            android:padding="12dp"
            android:layout_marginBottom="24dp"
            android:backgroundTint="@color/p_gtextbox"
            android:scrollbars="vertical"/>

        <!-- Delete button -->
        <Button
            android:id="@+id/btnDeleteAccount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Delete my account"
            android:textColor="@android:color/white"
            android:backgroundTint="@color/purple_bg"
            android:paddingStart="24dp"
            android:paddingEnd="24dp"
            android:layout_marginTop="16dp"/>
    </LinearLayout>


</androidx.constraintlayout.widget.ConstraintLayout>